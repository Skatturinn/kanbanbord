// import { group, notandi, project } from "@/types/types";

// function filterTruthyValues(obj: object) {
// 	const newObj: { [key: string]: string | number | boolean } = {};
// 	for (const key in obj) {
// 		if (obj[key]) {
// 			newObj[key] = obj[key];
// 		}
// 	}
// 	return newObj;
// }